server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/notobject/config-center.git
          # search-paths:
          label: master
          # search-paths: configs